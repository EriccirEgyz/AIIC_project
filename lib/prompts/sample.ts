/**
 * 生成一段脱敏的"假科研经历"供 demo 使用,避免真人数据合规问题。
 * 用 liteModel 跑,便宜快。
 */

export const SAMPLE_SYSTEM_PROMPT = `你是一位帮学生准备保研材料的写作助手。

请生成一段 200-300 字的"科研经历"自述,要求:
- 第一人称,口语化(像本科生写的)
- 包含:课题名 / 个人角色 / 用的方法 / 拿到的结果 / 自己负责的部分
- 故意留一些细节模糊的地方(后面要被追问)
- 全部脱敏,不要出现真实人名、学校名、实验室名
- 仅输出经历正文,不要标题、不要前言后语`;

export function buildSampleUserPrompt(field: string) {
  return `请为一位申请保研的本科生生成一段 ${field} 方向的科研经历样例。`;
}
