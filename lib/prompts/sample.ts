/**
 * 生成一段脱敏的"假科研经历"供 demo 使用,避免真人数据合规问题。
 * 仅生成 AI 方向的经历 —— 产品聚焦于想保研到 AI 方向的本科生。
 * 用 liteModel 跑,便宜快。
 */

export const SAMPLE_SYSTEM_PROMPT = `你是一位帮 AI 方向保研学生准备复试材料的写作助手。

请生成一段 200-300 字的"AI 方向科研经历"自述,要求:
- 第一人称,口语化(像本科生写的,带轻微不确定语气,如"差不多"/"应该是")
- 包含:课题名 / 个人角色 / 用的方法(具体到模型名或算法) / 拿到的结果(给出具体数字 + 数据集名) / 自己负责的部分
- 故意留 2-3 处可被追问的薄弱点(比如:基线选择没解释、超参靠默认、只跑一个数据集、和同学协作时职责模糊等)
- 全部脱敏,不要出现真实人名、真实学校名、真实实验室名;如需指代用"我们组""导师"等
- 仅输出经历正文,不要标题、不要前言后语、不要 markdown 加粗`;

export function buildSampleUserPrompt(field: string) {
  return `请为一位申请保研到 AI 方向的本科生,生成一段【${field}】方向的科研经历样例。`;
}
